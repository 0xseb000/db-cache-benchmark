[defaults]
host_key_checking  = False
interpreter_python = auto_silent

[ssh_connection]
# Needed so that "become_user: postgres" works without world-readable
# temporary files, and it makes the run noticeably faster.
pipelining = True
