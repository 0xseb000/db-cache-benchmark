# {{ ansible_managed }}
#
# Only what the lab actually needs: local maintenance access, loopback, and the web VM.

# TYPE  DATABASE            USER            ADDRESS             METHOD
local   all                 all                                 peer
host    all                 all             127.0.0.1/32        scram-sha-256
host    {{ pg_database }}   {{ pg_user }}   {{ web_host }}/32   scram-sha-256
